import { CheckOptionsDirective } from './check-options.directive';

describe('CheckOptionsDirective', () => {
  it('should create an instance', () => {
    const directive = new CheckOptionsDirective();
    expect(directive).toBeTruthy();
  });
});
